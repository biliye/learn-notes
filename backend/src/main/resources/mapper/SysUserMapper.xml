<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learnnotes.auth.mapper.SysUserMapper">

    <sql id="cols">
        id, username, password_hash, nickname, created_at, updated_at
    </sql>

    <select id="findByUsername" resultType="com.learnnotes.auth.SysUser">
        SELECT
        <include refid="cols"/>
        FROM sys_user WHERE username = #{username} LIMIT 1
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM sys_user
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password_hash, nickname)
        VALUES (#{username}, #{passwordHash}, #{nickname})
    </insert>
</mapper>
